const { EmailAddress } = require("@mergeapi/merge_ats_api")
const { HttpStatusCode } = require("axios")
const { serviceconsumermanagement } = require("googleapis/build/src/apis/serviceconsumermanagement")

const { xml } = require("cheerio")

- Adduction bouchée -> Génie civil (fouillle ou plus complexe) X
- Pas de lien entre la chambre et la boite piquage -> Génie civil X
- Passage cables -> Client (gel commande) X
- Travaux en hauteur -> Client (gel commande) X
- Alimentation électrique -> rallonge ou gel commande X
-Pas de disponibilité dans la baie-> proposition client ou gel commande X
-Pas de boite de piquage -> devis pour extension X

Mail 
- position client
-Photos MAC EmailAddress
-Facturation

Alignement 
Mise en service
Mesureur

-POP saturé
-Cable HS
-Fibre HS 
-Fibre occupée
-Problème POP non déclarer


-Vérifier chambre d'adduction ✓
-Adduction chez client ✓
-Passage baie informatique ✓
-Alimentation électrique ✓
-Dispo dans le baie ✓
-Boite de piquage ✓